# LunarRender 项目申请书（草稿）

## 一、项目名称

LunarRender：基于 MoonBit 的跨后端实时渲染运行时

## 二、项目摘要

LunarRender 是一个使用 MoonBit 构建的轻量级实时渲染运行时，目标是把窗口平台、渲染数据、GPU 资源生命周期和具体图形 API 进行分层，为游戏、可视化工具和模型预览程序提供统一的渲染基础。项目首个后端采用 Windows native、GLFW 3.4、GLAD 2.0.8 和 OpenGL 3.3 Core，但 OpenGL 只属于第一种后端实现，不是项目本身的功能边界。

项目将游戏规则与渲染运行时分离。MoonMC 负责世界、玩家、物理、方块和游戏交互；LunarRender 负责接收通用网格和帧数据并完成绘制。这样可以在不改变游戏逻辑的情况下替换 OpenGL、软件渲染或后续其他图形后端。

## 三、项目背景与意义

传统小型游戏项目经常把窗口、输入、世界逻辑、玩家状态、HUD 和具体图形 API 写在同一个主循环中，导致渲染接口只能服务于一个游戏，难以测试和复用。图形 API 的生命周期、资源所有权和错误处理也容易散落在业务代码中。

LunarRender 通过 MoonBit 的类型系统和包边界建立纯数据接口，再由独立后端实现 GPU 操作。项目可以作为 MoonBit 在实时图形、跨后端接口、C FFI 生命周期管理和工程化测试方面的实践样本。

## 四、项目目标

1. 建立不依赖具体游戏的 MoonBit 渲染数据接口。
2. 将窗口平台、渲染核心和 OpenGL 后端分开。
3. 实现网格上传、矩阵提交、帧绘制和资源销毁的完整生命周期。
4. 提供一个不包含 Minecraft 游戏规则的最小渲染示例。
5. 保留后续接入软件渲染、Vulkan 或其他后端的接口空间。
6. 建立可重复的 native 构建、测试和运行日志验证流程。

## 五、技术路线

```text
应用或游戏
  → 通用 MeshData / FrameData
  → LunarRender renderer
  → 选定的 Renderer Backend
  → 平台窗口与图形 API
```

### 1. renderer 公共层

公共层使用纯 MoonBit 数据结构表达网格和帧矩阵，不包含 OpenGL 句柄、窗口指针、C 指针和具体游戏字段。网格第一版采用位置、UV、明暗三个顶点属性，后续可以扩展法线、颜色、骨骼和实例数据。

### 2. OpenGL 后端

OpenGL 后端负责 GLAD 加载、Shader、VAO、VBO、纹理、绘制状态和资源销毁。GLFW 负责 Windows 窗口、输入和事件；C FFI 符号使用明确的 `lunarrender_opengl_*` 和 `lunarrender_platform_*` 前缀。

### 3. 可选模型格式模块

PMX 等模型格式解析器作为独立格式模块存在，只把文件转换为通用模型数据，不决定模型在具体游戏中的角色、位置或动画控制。私有模型文件不进入公共 Git 产物。

## 六、项目创新点

1. 使用 MoonBit 作为渲染公共层和数据边界语言，减少游戏逻辑与 C 图形实现的耦合。
2. 将“渲染接口”和“图形 API 后端”分离，OpenGL 只是第一种实现。
3. 通过明确的资源所有权和反向销毁顺序降低 FFI 泄漏和 context 生命周期错误。
4. 将游戏规则、模型格式、资源策略和 GPU 操作分别归属到不同层。
5. 使用 native 测试、CMake 构建和真实窗口日志进行工程验收，而不是只验证源码编译。

## 七、项目范围

### 本项目包含

- 通用网格和帧数据接口。
- Windows 窗口与输入平台层。
- OpenGL 3.3 Core 后端。
- Shader、VAO、VBO、纹理和资源生命周期。
- 最小静态网格渲染示例。
- PMX 等可选格式解析边界。
- 构建、测试、错误报告和资源所有权文档。

### 本项目不包含

- Minecraft 世界和区块生成。
- 玩家、重力、碰撞、跳跃和方块编辑。
- HUD、物品栏、命令行和游戏存档规则。
- 无限世界、联网和实体 AI。
- 私有模型、私有纹理和凭据。

## 八、实施计划

### 第一阶段：边界重构

完成 `renderer` 纯数据接口，去除 LunarRender 对 MoonMC 的直接依赖，固定平台层和后端层边界。

### 第二阶段：OpenGL 后端

拆分 OpenGL C FFI，完成通用网格槽位、帧矩阵提交、错误日志和资源销毁。

### 第三阶段：运行示例

提供静态三角形或立方体示例，验证窗口创建、后端初始化、网格上传、绘制和退出销毁。

### 第四阶段：格式扩展

隔离 PMX 解析和通用模型数据，为骨骼、材质和其他模型格式保留扩展边界。

### 第五阶段：后端扩展

在公共数据接口稳定后，评估软件渲染、Vulkan 或其他后端，不把后端实验代码倒灌到公共层。

## 九、预期成果

1. `PingGuoMiaoMiao/LunarRender` 开源仓库。
2. LunarRender 模块、架构文档、FFI 文档和构建脚本。
3. 可运行的 Windows native OpenGL 示例。
4. MoonBit 单元测试和 CMake/MSVC 构建记录。
5. 一个可以继续接入游戏或可视化工具的通用渲染接口。
6. 项目申请书、技术路线和阶段验收记录。

## 十、风险与应对

| 风险 | 应对措施 |
|---|---|
| 图形 API 差异导致公共接口过度抽象 | 第一版只固定最小网格和帧接口，后续按真实后端能力扩展 |
| C FFI 资源泄漏 | 统一初始化顺序和反向销毁顺序，增加失败路径日志 |
| 顶点格式扩展导致接口破坏 | 使用顶点格式字段和版本化数据结构，避免把游戏字段写入渲染接口 |
| 私有模型影响公共构建 | 私有模型只通过本地准备脚本使用，并加入 Git 忽略规则 |
| Windows 工具链差异 | 固定 MSVC、CMake、GLFW 和 GLAD 版本，并保留真实构建命令 |

## 十一、参考项目

- bgfx：跨平台、图形 API 无关的渲染库。
- sokol：轻量级跨平台图形和应用层封装。
- wgpu：通过核心层、硬件抽象层和具体后端组织图形实现。
- ClassiCube：Minecraft 风格游戏客户端和多平台实现参考。
- MoonBit 官方 FFI 与模块配置文档。

## 十二、说明

本文是根据当前仓库实现、已确认的项目边界和公开技术资料整理的项目申请书草稿。申请表中涉及申请人、指导教师、经费、院系和学校固定格式的字段，需要根据正式申报模板补填；本文不虚构这些信息。

