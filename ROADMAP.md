# LunarRender Roadmap

## 阶段 0：独立客户端文档和模块基线

状态：实现中。

固定模块版本、MoonMC 依赖、Windows/MSVC/CMake、GLFW/GLAD/OpenGL 版本、FFI 前缀、资源生命周期和私有素材规则。

验收：五个项目文档非空，所有权没有冲突，默认构建不依赖私有 MMD。

## 阶段 1：体素客户端迁移

状态：实现中。

范围：窗口、输入、GLAD、OpenGL 资源、资源包、区块/玩家上传、HUD、选中框、第一/第三人称和 MoonMC 主循环适配。

验收：CMake、MoonBit native 检查/测试和 Release 构建通过；实际窗口能初始化 OpenGL 3.3 Core、上传图集/区块/玩家网格并按顺序销毁资源。

## 阶段 2：纹理和网格表现

范围：严格横向图集、更多许可材质、透明层状态、批量上传和 Greedy Meshing。固定场景记录顶点数、帧时间和材质采样结果。

## 阶段 3：玩家和 HUD 完善

范围：更接近 Minecraft 的皮肤层、手臂视图、动画、选择框、热键栏、命令反馈和 UI 缩放。

## 阶段 4：可选 MMD 后端

范围：独立 PMX 解析、VMD/骨骼动画、材质和纹理；默认包只包含无私有素材的解析代码，启用后只通过本地准备脚本生成和链接。

验收：没有模型文件时基础客户端构建不变；有授权本地素材时才运行模型专项验证。

## 阶段 5：后续图形后端

OpenGL 是当前唯一实现。软件渲染、Vulkan 或其他后端只能通过独立 Renderer 实现，不改变 MoonMC 核心数据接口。
